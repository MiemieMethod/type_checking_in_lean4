# 信任

Lean 的核心价值之一，是能够构造数学证明，其中也包括关于程序正确性的证明。用户经常提出的一个问题是：在信任 Lean 时，我们究竟需要信任多少东西，又具体信任什么？

这个问题的答案分为两部分：为了信任 Lean 中的证明，用户需要信任什么；以及为了信任由 Lean 程序编译得到的可执行程序，用户又需要信任什么。

具体而言，二者的区别在于，证明（包括关于程序的命题）以及未编译的程序可以直接用 Lean 的内核语言表示，并由某个内核实现来检查。它们不需要被编译成可执行文件，因此信任范围仅限于用于检查它们的那个内核实现；Lean 编译器不会成为可信代码基的一部分。

信任已编译 Lean 程序的正确性，则要求信任 Lean 编译器；编译器与内核相分离，并不属于 Lean 的核心逻辑。信任 Lean 中的“关于程序的命题”，与信任“由 Lean 编译器产生的程序”，二者并不相同。关于 Lean 程序的命题是证明，属于只需信任内核的范畴。若要信任关于某个程序的证明能够延伸到该程序编译后的行为，就必须将编译器纳入可信代码基。

**注意**：策略以及其他元程序，即使是已编译的策略，也完全不需要被信任；它们是不可信代码，其作用只是产生供其他部分使用的内核项。一个命题 `P` 可以在 Lean 中借助任意复杂的已编译元程序得到证明，而无需把可信代码基扩展到内核之外，因为该元程序必须产生一个以 Lean 内核语言表示的证明。

+ 这些说法适用于已被[导出](../export_format.md)的证明。为了满足更为严谨的读者，这当然也意味着还要在一定程度上信任例如运行导出器与验证器的计算机上的操作系统、硬件等。

+ 对于未导出的证明，用户还额外信任内核之外的 Lean 组成部分（ elaborator、解析器等）。

## 更细的条目化说明

关于 Lean 4 中涉及的信任，Mario Carneiro 曾在 Lean Zulip 上给出过更细的说明。

> 一般而言：
>
> 1. 你信任 Lean 的逻辑是可靠的（作者注：这也包括任何内核扩展，例如用于 Nat 和 String 的扩展）。
>
> 2. 如果你没有证明程序正确，那么你信任 elaborator 已经把你的输入转换成了表示你所期望程序的 Lean 表达式。
>
> 3. 如果你已经证明程序正确，那么你信任关于该程序的证明已经被检查过（可使用外部检查器消除这一点）。
>
> 4. 你信任运行所有这些东西的硬件、固件和操作系统软件没有损坏，也没有欺骗你。
>
> 5. （运行程序时）你信任硬件、固件和操作系统软件按照规格忠实地执行程序，并且没有调试器、硬盘磁铁或宇宙射线干扰你的输出。
>
> 对于已编译的可执行文件：
>
> 6. 你信任任何编译器覆盖项（extern / implemented_by）没有违反 Lean 逻辑（即模型与实现相匹配）。
>
> 7. 你信任 Lean 编译器（它把 Lean 代码降为 C）会保持程序语义。
>
> 8. 你信任 clang / LLVM 会把 C 程序转换为具有相同语义的可执行文件。

第一组要点同时适用于证明和已编译的可执行文件；第二组则专门适用于已编译的可执行程序。

## 外部检查器中的信任

1. 你仍然信任 Lean 的逻辑是可靠的。

2. 你信任外部检查器的开发者正确地实现了这个程序。

3. 你信任实现语言的编译器或解释器。若运行多个外部检查器，可以把它们想象成维恩图中的若干圆：你信任这些圆的交集部分不存在可靠性问题。

4. 对于 Nat 和 String 的内核扩展，你大概还信任一个大整数库，以及实现语言中的 UTF-8 字符串类型。

使用外部检查器的优点包括：

+ 用户可以用某个与 Lean 生态完全相互独立、且不依赖 Lean 代码库任何部分的东西来检查自己的结果。

+ 外部检查器可以被写成利用成熟的编译器或解释器。

+ 对于内核扩展，用户可以交叉检查多个大整数/字符串实现的结果。

+ 使用导出功能是摆脱对 Lean 内核之外部分的信任的唯一方式。因此，即便导出文件由 [lean4lean](https://github.com/digama0/lean4lean/tree/master) 这样的工具检查，这样做也仍然有价值。因误用 Lean 元编程功能而担心后果的用户，因此应当考虑使用导出功能。
